# Employee-Management-System-Java
CLI-based custom DBMS simulation built using Java LinkedLists. It supports full CRUD operations (Insert, Select, Update, Delete) and SQL-style aggregate functions (Sum, Avg, Max, Min) to manage employee records efficiently in memory.
